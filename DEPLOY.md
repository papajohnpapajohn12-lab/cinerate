# 🚀 Деплой CineRate

## Шаг 1: Деплой Backend на Render

1. Перейдите на [render.com](https://render.com) и зарегистрируйтесь
2. Нажмите **"New +"** → **"Web Service"**
3. Подключите GitHub репозиторий или используйте "Public Git repository"
4. Настройте:
   - **Name**: `cinerate-api` (или любое другое)
   - **Runtime**: `Python 3`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `uvicorn main:app --host 0.0.0.0 --port $PORT`
5. Добавьте Environment Variables:
   ```
   TURSO_DATABASE_URL=libsql://your-db.turso.io
   TURSO_AUTH_TOKEN=your-token
   SECRET_KEY=your-secret-key
   TMDB_API_KEY=your-tmdb-key
   ```
6. Нажмите **"Create Web Service"**
7. Дождитесь деплоя и скопируйте URL (например: `https://cinerate-api.onrender.com`)

## Шаг 2: Обновите API URL

В файле `frontend/js/api.js` замените URL:
```javascript
const API_URL = window.location.hostname === 'localhost' 
    ? 'http://localhost:8000'
    : 'https://your-service-name.onrender.com'; // <-- Ваш URL
```

## Шаг 3: Деплой Frontend на Vercel

1. Перейдите на [vercel.com](https://vercel.com) и зарегистрируйтесь (можно через GitHub)
2. Нажмите **"Add New..."** → **"Project"**
3. Импортируйте репозиторий с кодом
4. Настройки:
   - **Framework Preset**: `Other`
   - **Build Command**: оставьте пустым
   - **Output Directory**: оставьте пустым
5. Нажмите **"Deploy"**

## Готово! 🎉

Ваш сайт будет доступен по URL типа `https://cinerate-xxx.vercel.app`

## 📱 Доступ с мобильного

После деплоя сайт будет доступен:
- С любого устройства через интернет
- Можно добавить на домашний экран как PWA
- Данные синхронизируются через облачную БД Turso

## 🔧 Важно

- Backend на Render "засыпает" после 15 минут неактивности (бесплатный план)
- Первый запрос после "сна" может занять 30-60 секунд
- Для постоянной работы рассмотрите платный план ($7/месяц)
