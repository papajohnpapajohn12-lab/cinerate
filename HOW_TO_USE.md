# 🎬 CineRate — Как пользоваться

## Быстрый старт

### 1. Запуск backend (обязательно)

```bash
# Способ 1: Через batch файл
double-click start-server.bat

# Способ 2: Вручную
cd backend
.\venv\Scripts\activate
python run.py
```

API будет доступен по адресу: `http://localhost:8000`
Документация API: `http://localhost:8000/docs`

### 2. Запуск frontend

```bash
# Способ 1: Через batch файл
double-click start-frontend.bat

# Способ 2: Вручную
python -m http.server 3000
```

Открыть в браузере: `http://localhost:3000`

Или просто откройте `index.html` в браузере.

## Функционал

### 🔐 Авторизация
- Регистрация нового аккаунта
- Вход по username/password
- JWT токены (срок действия 24 часа)
- Данные сохраняются в облачной БД Turso

### 🎬 Фильмы и сериалы
- Поиск через TMDB API
- Просмотр популярных и топовых фильмов
- Детальная информация (постер, описание, рейтинг)
- Поддержка фильмов и сериалов

### ⭐ Оценки
- Оценка от 1 до 10 звёзд
- Личная коллекция оценённых фильмов
- Фильтрация: все, фильмы, сериалы, по рейтингу
- Редактирование и удаление оценок

### 📊 Статистика
- Общее количество оценок
- Средняя оценка
- Лучшая/худшая оценка
- Распределение оценок (график)
- Топ-10 фильмов
- **Любимые жанры** (с графиком)
- **Статистика по типам** (фильмы vs сериалы)

### 💾 Экспорт данных
- Скачивание всех оценок в формате JSON
- Полезно для резервного копирования

## Доступ с разных устройств

Так как данные хранятся в облачной базе Turso:
1. Backend можно развернуть на Render/Railway/VPS
2. Или использовать туннель (ngrok/cloudflared) для локального сервера

### Пример с ngrok:
```bash
# Установите ngrok
# Запустите backend
# В другом окне:
ngrok http 8000
```

Полученный URL используйте в `frontend/js/api.js` вместо `http://localhost:8000`

## Переменные окружения

Файл `backend/.env`:
```env
TURSO_DATABASE_URL=libsql://your-db.turso.io
TURSO_AUTH_TOKEN=your-token
SECRET_KEY=your-secret
TMDB_API_KEY=your-tmdb-key
```

## Технологии

- **Backend**: FastAPI + Python
- **Database**: Turso (SQLite на edge)
- **Frontend**: Vanilla JavaScript + CSS3
- **API**: TMDB (The Movie Database)
