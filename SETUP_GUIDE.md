# 📋 Пошаговая инструкция запуска

## ШАГ 1: Создание базы данных в Turso

Открой терминал (cmd или PowerShell) и выполни:

```bash
# Войти в Turso (если давно не входил)
turso auth login

# Создать новую базу данных для проекта
turso db create cinerate-films

# Посмотреть URL базы (скопируй его)
turso db show cinerate-films
```

Увидишь что-то вроде:
```
URL:          libsql://cinerate-films-ТВОЙ_НИК.turso.io
```

**Скопируй этот URL!**

Теперь создай токен для доступа:

```bash
turso db tokens create cinerate-films
```

Увидишь длинную строку — **это токен, тоже скопируй!**

---

## ШАГ 2: Получаем API ключ TMDB

1. Перейди на https://www.themoviedb.org/settings/api
2. Войди в аккаунт (или создай)
3. Нажми "Create" или "Request API Key"
4. Выбери "Developer"
5. Заполни название: "CineRate"
6. **Скопируй API Key (v3 auth)**

---

## ШАГ 3: Настраиваем backend

### 3.1 Создаём файл .env

В папке `D:\Programming\filmocenka\backend` создай файл `.env` (просто `.env`, без имени):

```env
TURSO_DATABASE_URL=libsql://cinerate-films-ТВОЙ_НИК.turso.io
TURSO_AUTH_TOKEN=твой_токен_из_шага_1
SECRET_KEY=любая_случайная_строка_например_kj2h3k4jh23kj4h
TMDB_API_KEY=твой_api_ключ_из_шага_2
```

**Замени значения на свои!**

### 3.2 Устанавливаем Python зависимости

Открой терминал в папке `backend`:

```bash
cd D:\Programming\filmocenka\backend

# Создать виртуальное окружение
python -m venv venv

# Активировать (Windows)
venv\Scripts\activate

# Установить библиотеки
pip install -r requirements.txt
```

### 3.3 Запускаем backend

```bash
python run.py
```

Если всё ок, увидишь:
```
✅ Database initialized successfully!
INFO:     Uvicorn running on http://0.0.0.0:8000
```

**Оставь это окно открытым!** ✅

Проверка: открой в браузере http://localhost:8000/docs — должна открыться документация API.

---

## ШАГ 4: Запускаем frontend

Просто открой файл `D:\Programming\filmocenka\index.html` в браузере (дважды кликни по нему).

Или если есть VS Code — установи расширение "Live Server" и нажми "Go Live".

---

## ШАГ 5: Проверяем

1. Открой http://localhost:8000/docs — API работает?
2. Открой index.html — видишь страницу регистрации?
3. Создай аккаунт и залогинься
4. Поищи фильм и поставь оценку

---

## ❌ Если ошибки

### "ModuleNotFoundError"
```bash
# Переустанови зависимости
pip install -r requirements.txt --force-reinstall
```

### "Cannot connect to Turso"
Проверь файл `.env` — правильно ли скопирован URL и токен?

### "Port 8000 is already in use"
```bash
# Найди и убей процесс (PowerShell)
Get-NetTCPConnection -LocalPort 8000
# Или просто перезагрузи компьютер
```

---

Когда выполнишь Шаг 1 (Turso) — напиши, пойдём дальше! 👍
